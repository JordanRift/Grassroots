@if (Request.IsAuthenticated) {
        <text>Welcome back</text> <strong>@Html.ActionLink(User.Identity.Name, "Index", new { controller = "UserProfile" }, new { title = "User Profile" })</strong><text>!</text>
        <text>[ </text> @Html.ActionLink("Log Off", "LogOff", "Account") <text> ]</text>
}
else {
        <text>[ </text> @Html.ActionLink("Log On", "LogOn", "Account") <text> ]</text>
}